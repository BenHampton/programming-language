# U-DirtyBit Language
### node versions: 
    v20.11.0
### npm versions: 
    10.2.4

### run Node:    
    ts-node src/main.ts

### run Deno:
    deno run -A main.ts

## Running the application
in `main.ts` isTerminal determines if the application run from user input via terminal or the test file in testFile